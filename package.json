{
  "name": "protocolos-ai-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "build": "node scripts/build.mjs && wrangler pages functions build functions --outdir=.build/functions && node scripts/check-public-build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler pages dev dist --port 5185",
    "rag:sync": "node --env-file-if-exists=.env.rag scripts/sync-public-rag.mjs"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
